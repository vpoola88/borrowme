<!-- <div class="index">
  <div class="index-body">
    <div class="container">
      <div class="row">
        <div class="col-xs-12">
          <h1 class="text-center">BorrowMe</h1>
        </div>
      </div>
    </div>
  </div>
</div>
 -->
<div id="search" class="container">
  <div class="row">
    <div class="col-xs-12 text-center">
      <h2>Search Results:</h2>
      <% if @search.first %>
        <% @search.each do |item| %>
        <p><%= item.name %></p>
        <p><%= item.id %></p>
        <p><%= item.category.title %></p>
        <p><%= item.category.id %></p>
        <% end %>
      <% else %>
        <h3>Couldn't Find That</h3>
      <% end %>
    </div>
  </div>
</div>
